apiVersion: v1
kind: Service
metadata:
  name: {{ include "log-processor.fullname" . }}
  labels:
    {{- include "log-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "log-processor.selectorLabels" . | nindent 4 }}
